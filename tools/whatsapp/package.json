{
  "name": "queens-whatsapp-inviter",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local CLI to invite known contacts to the African Global Queens Summit via the user's own WhatsApp. Personal-event invitations only.",
  "bin": {
    "queens-whatsapp": "./send.mjs"
  },
  "scripts": {
    "dry-run": "node send.mjs",
    "report": "node send.mjs --report",
    "selftest": "node send.mjs --selftest",
    "send": "node send.mjs --send"
  },
  "dependencies": {
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.23.0"
  }
}
