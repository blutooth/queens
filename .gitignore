node_modules
dist
.DS_Store
.vite
*.log

# Generated invitation pages (built from content/ by scripts/build-invites.mjs)
public/invite/

# Raw source material — kept locally, not published.
# (PDFs, original photos, working notes.)
reference/

# Local WhatsApp invitation tool (tools/whatsapp/)
tools/whatsapp/node_modules/
tools/whatsapp/.wwebjs_auth/
tools/whatsapp/.wwebjs_cache/
tools/whatsapp/sent.json

# Personal data (invitees, visa letters w/ passport & DOB, data files) is now
# COMMITTED to this repo. ⚠️ THIS REPO MUST REMAIN PRIVATE — it contains PII.
# Only the generated public build output stays ignored (regenerated at deploy):
public/visa/
public/v/

# never commit secrets
stripe.txt
*.secret
.dev.vars
